# Copia este archivo como ".env" y reemplaza los valores con tus credenciales reales.
# El archivo ".env" NUNCA debe subirse a git (ya está en .gitignore).
DATABASE_URL=postgresql://usuario:clave@localhost:5432/fraude_test
